{
  "name": "pdf-worker-api",
  "version": "1.0.0",
  "description": "API service for generating PDFs from public URLs using Puppeteer.",
  "main": "pdf-worker.js",
  "scripts": {
    "start": "node pdf-worker.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.6.1"
  }
}
