{
  "name": "pdf-worker-api",
  "version": "1.0.0",
  "description": "API service for generating PDFs from public URLs using Puppeteer-core for deployment compatibility.",
  "main": "pdf-worker.js",
  "scripts": {
    "start": "node pdf-worker.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^21.11.0",
    "chrome-aws-lambda": "^10.0.0" 
  }
}
